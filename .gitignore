.env
.superpowers/
__pycache__/
*.pyc
*.db
.venv/
node_modules/
.next/
.backtest_cache/

# Claude Code local state (settings + nested git worktrees, ~2MB)
.claude/

# Live trading data and generated output — regenerated by the bot, not source.
# The trade journal is deliberately NOT tracked: it changes on every close and
# would conflict constantly. Keep your own backups of it.
trade_journal.csv
entry_log.md
position_snapshots.md
baseline_*.csv

# Safety backups taken before destructive operations (*.db is already ignored,
# but the dated .bak- suffixes are not).
*.bak-*
